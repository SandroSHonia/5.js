function attachStrings(names) {
  
    const [first, , third] = names;
  
    return  `${first} , ${third}`;
}
  
  const names = ["ლაშა", "გვანცა", "ზაზა", "სპირიდონი"];

  console.log(attachStrings(names)); 














